<%= form_with(url: show_path, method: "get", local: true) do %>
  <%= label_tag(:username, "Search for:") %>
  <%= text_field_tag(:username, nil, placeholder: 'Github Name', autofocus: true) %>
  <br>
  <br>
  <%= submit_tag("Submit") %>
<% end %>